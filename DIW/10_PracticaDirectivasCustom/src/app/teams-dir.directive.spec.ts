import { TeamsDirDirective } from './teams-dir.directive';

describe('TeamsDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamsDirDirective();
    expect(directive).toBeTruthy();
  });
});
